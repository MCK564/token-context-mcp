{
  "servers": {
    "token-context": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run", "--no-sync",
        "--directory", "/absolute/path/to/token-context-mcp",
        "token-context", "serve", "--transport", "stdio"
      ]
    }
  }
}
